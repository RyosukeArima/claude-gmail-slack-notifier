使い方ガイドを表示する。

以下を表示すること:

```
Mail Notifier - 使い方ガイド

[基本コマンド]
  /mail-check   Gmailを今すぐチェックしてSlackに通知
  /mail-setup   Slack・メールフィルタ・GASの設定を変更
  /help         この使い方ガイドを表示

[返信の下書き作成]
  Slack通知の「下書き作成」ボタン → ワンクリックでGmailに下書き作成
  「3番の返信下書き作って」       → Claude Codeが下書きを生成

[自動通知]
  セッション中は毎時自動でメールをチェックします。
  セッションが切れた場合は claude を再起動してください。

[設定ファイル]
  config.json   メールフィルタ・GAS連携の設定（自動生成）
  state.json    最終チェック時刻（自動管理）
```
